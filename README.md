# P4-Project

To ensure implementation works, make sure the video files (Included in P4 Extras) are in the streaming assets folder (\Assets\StreamingAssets).
If the game runs slow, try reducing the kernel size of the post processing shader. You can do this by editing the Material "Kuwahara" in the "Materials & Shaders" folder.
